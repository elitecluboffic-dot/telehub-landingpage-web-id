{
  "name": "duojump",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-levels": "node scripts/generate-levels.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute picopark-db --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute picopark-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
